micronaut:
  server:
    port: -1
  application:
    banner:
      enabled: false
  aot:
    optimize-service-loading: false
    optimize-class-loading: false
    precompute-operations: false

datasources:
  default:
    enabled: false

jpa:
  default:
    enabled: false