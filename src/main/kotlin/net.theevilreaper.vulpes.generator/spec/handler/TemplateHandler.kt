package net.theevilreaper.vulpes.generator.spec.handler

/*import jakarta.websocket.server.PathParam
import net.theevilreaper.vulpes.generator.spec.database.TemplateDatabaseHandler
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.http.ResponseEntity
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.RequestMethod
import org.springframework.web.bind.annotation.RestController*/

//
class TemplateHandler {

  /*  @Autowired
    private lateinit var templateDatabaseHandler: TemplateDatabaseHandler

    @RequestMapping("template/download/{template}", method = [RequestMethod.GET], produces = ["application/octet-stream"])
    fun downloadTemplate(@PathParam("template") template: String): ResponseEntity<Any> {
        return ResponseEntity.ok().build()
    }*/
}