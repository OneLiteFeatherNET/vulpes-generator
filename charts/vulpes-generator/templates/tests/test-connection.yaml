apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "micronaut.fullname" . }}-test-connection"
  labels:
    {{- include "micronaut.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "micronaut.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
